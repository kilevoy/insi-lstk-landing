// Optional HTTPS endpoint compatible with the multipart request contract.
// Never place API keys or other secrets in this public file.
// Empty endpoint: the form prepares an EML draft with attachments for manual sending.
window.INSI_CONFIG = { quoteEndpoint: '' };
